version: '3.8'

services:
  db:
    image: postgres:latest
    restart: always
    environment:
      POSTGRES_DB: soul-api
      POSTGRES_USER: soul-api
      POSTGRES_PASSWORD: soul-api
    ports:
      - "5432:5432"